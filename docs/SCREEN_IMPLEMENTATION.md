# 画面別実装状況 — オリジナル対照表

オリジナル: `original/CRUDExplorer2011/*.vb`  
新実装: `src/CRUDExplorer.UI/**`

凡例: ✅ 完了 / ⚠️ 不完全 / ❌ 未実装

---

## 1. MainWindow（frmMain.vb）

### メニュー

| オリジナル                      | 現在                         | 状態 |
| ------------------------------- | ---------------------------- | ---- |
| テーブルCRUD（フォルダ選択+表示）| ファイル > フォルダを開く     | ⚠️ 種別切替と一体化が不完全 |
| カラムCRUD（フォルダ選択+表示）  | 表示 > カラムCRUD            | ⚠️ フォルダ選択なし |
| CRUD解析なし（フォルダを開くのみ）| —                           | ❌ 未実装 |
| CRUD解析実行（frmMakeCRUD）      | 解析 > CRUD解析実行          | ✅ |
| テーブル定義（frmTableDef）      | 解析 > テーブル定義読み込み   | ⚠️ ウィンドウを開かない |
| ソースファイル一覧（frmFileList）| —                           | ❌ 未実装 |
| クエリ表示（frmAnalyzeQuery）    | —                           | ❌ 未実装 |
| フィルタ詳細（frmFilter）        | 表示 > フィルタ              | ⚠️ フィルタが Matrix に反映されない |
| 設定（frmSettings）              | 設定 > 設定                  | ✅ |
| バージョン情報（frmVersion）     | ヘルプ > バージョン情報       | ✅ |
| サポートサイト（ブラウザ起動）    | —                           | ❌ 未実装 |

### ツールバー / ボタン

| オリジナル                       | 現在                      | 状態 |
| -------------------------------- | ------------------------- | ---- |
| フォルダ選択ボタン                | ✅ あり                   | ✅ |
| CRUD解析ボタン                   | ✅ あり                   | ✅ |
| テーブル定義ボタン                | ✅ あり（読み込みのみ）    | ⚠️ |
| フィルタ適用ボタン                | —                         | ❌ 未実装 |
| フィルタクリアボタン              | —                         | ❌ 未実装 |
| プログラムIDフィルタ入力欄        | フィルタテキスト（共用）   | ⚠️ 別途実装が必要 |
| テーブル名フィルタ入力欄          | フィルタテキスト（共用）   | ⚠️ 別途実装が必要 |
| C/R/U/Dチェックボックス          | —                         | ❌ 未実装 |
| エディタ起動ボタン                | —                         | ❌ 未実装 |
| クエリ解析ボタン                  | —                         | ❌ 未実装 |
| テーブル定義表示ボタン（CRUD一覧から）| —                      | ❌ 未実装 |
| 論理名/物理名切替ボタン           | —                         | ❌ 未実装 |
| マトリクスコピー（クリップボード） | —                         | ❌ 未実装 |

### DataGrid（CRUDマトリクス）インタラクション

| オリジナル                             | 現在             | 状態 |
| -------------------------------------- | ---------------- | ---- |
| **セル表示**（動的列バインディング）    | 空欄になる       | ❌ **バグ** |
| セルクリック → CRUD一覧更新（テーブル×プログラム） | — | ❌ 未実装 |
| 行ヘッダクリック → CRUD一覧更新（テーブル全プログラム） | — | ❌ 未実装 |
| 列ヘッダクリック → CRUD一覧更新（プログラム全テーブル） | — | ❌ 未実装 |
| カラムヘッダ縦書き表示               | —               | ❌ 未実装（Avalonia 制限） |

### CRUD一覧（右ペイン）インタラクション

| オリジナル                       | 現在                     | 状態 |
| -------------------------------- | ------------------------ | ---- |
| 全CRUD一覧を表示                 | ✅ 全件表示               | ⚠️ セル選択との連動なし |
| ダブルクリック → エディタ起動    | ステータス更新のみ        | ❌ 未実装 |
| ダブルクリック → クエリ解析      | —                        | ❌ 未実装 |
| 列クリック → ソート              | —                        | ❌ 未実装 |

---

## 2. MakeCrudWindow（frmMakeCRUD.vb）

| 機能                          | 状態 |
| ----------------------------- | ---- |
| ソース/出力フォルダ選択       | ✅ |
| Step 0–4 実行                 | ✅ |
| 進捗ログ表示                  | ✅ |
| 全ステップ/個別ステップ選択   | ✅ |
| VIEW反映・参照条件チェック    | ✅（設定は保持、動作は未検証） |

---

## 3. FilterWindow（frmFilter.vb）

| 機能                                 | 状態 |
| ------------------------------------ | ---- |
| プログラムIDリスト＋チェック選択     | ❌ 未実装（シンプルなテキスト入力のみ） |
| テーブル名リスト＋チェック選択       | ❌ 未実装（テキスト入力のみ） |
| プログラムアクセスComboBox           | ❌ 未実装 |
| テーブルアクセスComboBox             | ❌ 未実装 |
| 適用 → Matrix 再読み込み            | ❌ Apply がウィンドウを閉じるだけ |
| キャンセル → 変更なし               | ✅ |

---

## 4. AnalyzeQueryWindow（frmAnalyzeQuery.vb）

| 機能                            | 状態 |
| ------------------------------- | ---- |
| ウィンドウ表示・基本構造        | ✅ |
| クエリ解析ツリー表示            | ⚠️ 詳細実装は前フェーズで実施 |

---

## 5. SettingsWindow（frmSettings.vb）

| 機能                            | 状態 |
| ------------------------------- | ---- |
| 全設定項目の表示・保存          | ✅ |
| エディタパス参照ボタン          | ✅ |

---

## 6. TableDefinitionWindow（frmTableDef.vb）

| 機能                                  | 状態 |
| ------------------------------------- | ---- |
| ウィンドウ表示                        | ✅ |
| MainWindow から「テーブル定義」コマンドで開く | ❌ 現在はファイル読み込みのみ |

---

## 7. FileListWindow（frmFileList.vb）

| 機能                                        | 状態 |
| ------------------------------------------- | ---- |
| MainWindow メニュー「ソースファイル一覧」から開く | ❌ メニュー未実装 |
| ファイル一覧表示                            | ⚠️ ViewModel は実装済み、メニューがない |

---

## 8. GrepWindow（frmGrep.vb）

| 機能                          | 状態 |
| ----------------------------- | ---- |
| ウィンドウ表示                | ✅ ViewModel 実装済み |
| MainWindow メニューからの起動 | ❌ メニュー未実装 |

---

## 9. CrudSearchWindow（frmCRUDSearch.vb）

| 機能                          | 状態 |
| ----------------------------- | ---- |
| ウィンドウ表示                | ✅ ViewModel 実装済み |
| MainWindow メニューからの起動 | ❌ メニュー未実装 |

---

## 10. VersionWindow（frmVersion.vb）

| 機能                          | 状態 |
| ----------------------------- | ---- |
| ライセンス情報表示            | ✅ |
| 認証トークン保存              | ✅ |

---

## 11. StartupWindow（frmStartup.vb）

| 機能             | 状態 |
| ---------------- | ---- |
| スプラッシュ表示 | ✅ |

---

## 実装優先順位

1. **[Critical] MainWindow DataGrid バインディング修正** — セルが空欄になるバグを修正
2. **[High] MainWindow セルクリック → CRUD一覧フィルタ** — マトリクスの基本インタラクション
3. **[High] MainWindow 不足メニュー追加** — ソースファイル一覧・クエリ表示・サポートサイト
4. **[High] MainWindow ツールバー補完** — フィルタ入力・C/R/U/Dチェック・エディタ/クエリボタン
5. **[Medium] FilterWindow 機能強化** — マトリクスへの実フィルタ反映
6. **[Medium] TableDefinitionWindow 起動コマンド修正** — メニューから直接開く
7. **[Low] FileList/Grep/CrudSearch メニュー追加** — 既存ViewModel を利用
