{
  "sourceFile": "csharp_adonet_sample.cs",
  "description": "C# ADO.NET SqlCommand",
  "expectedResults": [
    { "sql": "SELECT id, name, email FROM users WHERE id = @id", "tables": { "users": ["R"] } },
    { "sql": "INSERT INTO users (name, email) VALUES (@name, @email)", "tables": { "users": ["C"] } },
    { "sql": "UPDATE users SET email = @email WHERE id = @id", "tables": { "users": ["U"] } },
    { "sql": "DELETE FROM users WHERE id = @id", "tables": { "users": ["D"] } }
  ]
}
